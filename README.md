In order to use readorsee as a package, do the following steps

1) Activate any desirable virtual environment: `source <env>/bin/activate`
2) Install the package: go to the root folder and write: `pip install -e .`
